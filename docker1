# Use Nginx to serve the frontend static files
FROM nginx:alpine

# Copy the frontend files into the Nginx directory
COPY ./ /usr/share/nginx/html/

# Expose port 8080 for the web server
EXPOSE 8080
